[build]
builder = "NIXPACKS"

[phases.setup]
aptPkgs = ["ghostscript", "qpdf", "libreoffice-writer", "libreoffice-calc", "tesseract-ocr", "tesseract-ocr-eng", "fonts-liberation"]

[start]
cmd = "npm start"